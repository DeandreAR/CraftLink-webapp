# Archivé — déploiement Docker / VPS (non utilisé en V1 Vercel)
#
# CraftLink V1 est déployé sur Vercel. Ces fichiers restent pour une éventuelle
# auto-hébergement future. Vercel les ignore (pas de Dockerfile à la racine).
